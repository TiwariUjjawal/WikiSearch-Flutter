class Page {
  int pageId;
  String title;
  String imageURL;
  String description;

  Page(
      {required this.pageId,
      required this.title,
      required this.imageURL,
      required this.description});
}
